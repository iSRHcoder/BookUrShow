export const diamondSeats = Array.from({ length: 15 }, (_, index) => ({
  seatNo: index + 1,
  isBooked: false,
}));
export const goldenSeats = Array.from({ length: 45 }, (_, index) => ({
  seatNo: index + 1,
  isBooked: false,
}));
export const silverSeats = Array.from({ length: 60 }, (_, index) => ({
  seatNo: index + 1,
  isBooked: false,
}));
